# 1. 学术期刊绘图全局配置 (PRL/Nature 标准)
# ==========================================
PLOT_CONFIG = {
    "font_family": "Arial",      # 推荐字体：Arial 或 Helvetica
    "font_size": 10,             # 全局基础字体大小 (正文一般为 8-10)
    "axes_width": 1.0,           # 坐标轴外框边线的粗细
    "tick_size": 4,              # 刻度线的长度
    "line_width": 0.8,           # 默认折线粗细 (0.8 适合密集数据，显得精致)
    "line_color": "#9D0A12",     # 默认折线颜色 (你专属的深红色)
    "dpi": 300,                  # 输出图片的分辨率 (300+ 满足印刷和审稿要求)
    "fig_size_single": (3.375, 2.5), # 单张谱线的标准画布尺寸 (宽x高，英寸。3.375 约合 8.6cm 单栏宽)
    "fig_size_waterfall": (4.0, 5.5) # 瀑布图的标准画布尺寸 (堆叠线多，通常需要更高的纵向空间)
}

# 2. 物理量与单位换算配置
# ==========================================
PHYSICS_CONFIG = {
    "x_multiplier": 1000,        # X轴乘数：将 Nanonis 原始的 V 转换为 mV
    "y_multiplier": 1,        # Y轴乘数：将原始信号 (通常是 1e-12 级别) 放大，消除坐标轴的科学计数法
    "x_label": r'Bias Voltage $V_b$ (mV)', # X轴物理标签 (支持 LaTeX 数学公式渲染)
    "y_label": r'$dI/dV$ (arb. units)'     # Y轴物理标签 (瀑布图由于进行了平移，绝对数值失去意义，通常用 arb. units)
}

# 3. 瀑布图 (平移堆叠) 专属高级配置
# ==========================================
WATERFALL_CONFIG = {
    # --- 核心空间参数 ---
    "offset_step": 2.0,          # 纵向平移步长 🌟 (关键参数：如果谱线互相重叠看不清，改大；如果间距太大太空旷，改小)
    
    # --- 科学平滑配置 ---
    "enable_smoothing": True,    # 是否开启平滑曲线对比
    "smooth_window": 11,         # 窗口长度（必须是正奇数，越大越平滑）
    "smooth_polyorder": 3,       # 多项式拟合阶数（通常取 2 或 3）
    "raw_alpha": 0.3,            # 原始数据的透明度（0-1，越小越淡）
    "raw_line_width": 0.5,       # 原始数据的线宽（建议比平滑线细）
    "smooth_line_width": 1.5,    # 平滑曲线的线宽（建议比原始线宽更粗，突出对比）
    "smooth_color": "#2A9D8F",  # 平滑曲线的颜色 (推荐使用与原始线颜色形成对比的颜色)
    
    # --- 文本标注参数 ---
    "show_label": True,          # 是否在每条谱线最右侧显示 #1, #2... 的标注
    "label_offset_x": 5,         # 标注文本距离谱线最右端数据点的水平距离 (单位与 X 轴一致，这里即 5 mV)
    "label_font_size": 8,        # 标注文本的字体大小 (稍微比主字体小一点，显得主次分明)
    
    # --- 色彩方案参数 ---
    # 【模式 A】: 使用渐变色谱 (推荐用于展示随空间位置连续变化的 Linecut 数据)
    "use_colormap": True,        # 🌟 设为 True 则启用渐变色，此时下方的 color_cycle 将被忽略
    "colormap_name": "viridis",  # 推荐的科学渐变色：'viridis', 'plasma', 'inferno', 'magma' (这些颜色对色弱友好且打印清晰)
    
    # 【模式 B】: 使用自定义循环色 (推荐用于对比几个完全不同条件的独立样品)
    # 🌟 只有当你把上面的 "use_colormap" 改为 False 时，程序才会从下面这个列表里按顺序挑颜色
    "color_cycle": [
        "#9D0A12",               # 上科大红
        "#1D3557",               # 普鲁士蓝
        "#2A9D8F",               # 蓝绿
        "#E9C46A",               # 砂金
        "#F4A261",               # 橘黄
        "#6D6875"                # 莫兰迪紫
    ]
}